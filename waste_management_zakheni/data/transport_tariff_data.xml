<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- ================================================== -->
    <!-- FLAT RATE - SINGLE BIN -->
    <!-- ================================================== -->
    <record id="transport_tariff_flat_single_initial"
            model="waste.transport.tariff">

        <field name="name">Flat Rate - Single Bin</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">flat_single</field>

        <field name="date_from">2025-01-01</field>

        <field name="single_bin_price">500</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- FLAT RATE - MULTIPLE BINS -->
    <!-- ================================================== -->
    <record id="transport_tariff_flat_multiple_initial"
            model="waste.transport.tariff">

        <field name="name">Flat Rate - Multiple Bins</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">flat_multiple</field>

        <field name="date_from">2025-01-01</field>

        <field name="multiple_bin_price">750</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER BIN - SINGLE -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_bin_single_initial"
            model="waste.transport.tariff">

        <field name="name">Per Bin - Single Bin</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_bin_single</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_bin_rate">300</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER BIN - MULTIPLE -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_bin_multiple_initial"
            model="waste.transport.tariff">

        <field name="name">Per Bin - Multiple Bins</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_bin_multiple</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_bin_rate">300</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER TRIP -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_trip_initial"
            model="waste.transport.tariff">

        <field name="name">Per Trip</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_trip</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_trip_rate">700</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER BIN PER KM - SINGLE -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_bin_km_single_initial"
            model="waste.transport.tariff">

        <field name="name">Per Bin Per KM - Single Bin</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_bin_per_km_single</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_bin_rate">1</field>

        <field name="per_km_rate">5</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER BIN PER KM - MULTIPLE -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_bin_km_multiple_initial"
            model="waste.transport.tariff">

        <field name="name">Per Bin Per KM - Multiple Bins</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_bin_per_km_multiple</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_bin_rate">0</field>

        <field name="per_km_rate">5</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- PER TRIP PER KM -->
    <!-- ================================================== -->
    <record id="transport_tariff_per_trip_per_km_initial"
            model="waste.transport.tariff">

        <field name="name">Per Trip Per KM</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">per_trip_per_km</field>

        <field name="date_from">2025-01-01</field>

        <field name="per_trip_rate">1</field>

        <field name="per_km_rate">10</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED BY BIN COUNT - 1 BIN -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_bin_1_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered By Bin Count - 1 Bin</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_bin_1</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_bin_qty">1</field>

        <field name="max_bin_qty">1</field>

        <field name="rate">300</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED BY BIN COUNT - 2 TO 3 -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_bin_2_3_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered By Bin Count - 2 To 3 Bins</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_bin_2_3</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_bin_qty">2</field>

        <field name="max_bin_qty">3</field>

        <field name="rate">550</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED BY BIN COUNT - 4+ -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_bin_4_plus_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered By Bin Count - 4+ Bins</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_bin_4_plus</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_bin_qty">4</field>

        <field name="rate">800</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED DISTANCE - 0 TO 10 KM -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_distance_0_10_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered Distance - 0 To 10 KM</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_distance_0_10</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_distance">0</field>

        <field name="max_distance">10</field>

        <field name="rate">300</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED DISTANCE - 11 TO 30 KM -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_distance_11_30_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered Distance - 11 To 30 KM</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_distance_11_30</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_distance">11</field>

        <field name="max_distance">30</field>

        <field name="rate">600</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- TIERED DISTANCE - 31+ KM -->
    <!-- ================================================== -->
    <record id="transport_tariff_tier_distance_31_plus_initial"
            model="waste.transport.tariff">

        <field name="name">Tiered Distance - 31+ KM</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">tier_distance_31_plus</field>

        <field name="date_from">2025-01-01</field>

        <field name="min_distance">31</field>

        <field name="rate">1000</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- HYBRID - PER BIN + FLAT TRIP -->
    <!-- ================================================== -->
    <record id="transport_tariff_hybrid_bin_trip_initial"
            model="waste.transport.tariff">

        <field name="name">Hybrid - Per Bin + Flat Trip</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">hybrid_bin_trip</field>

        <field name="date_from">2025-01-01</field>

        <field name="base_rate">300</field>

        <field name="per_bin_rate">200</field>

        <field name="active">True</field>

    </record>

    <!-- ================================================== -->
    <!-- HYBRID - TRIP + KM -->
    <!-- ================================================== -->
    <record id="transport_tariff_hybrid_trip_km_initial"
            model="waste.transport.tariff">

        <field name="name">Hybrid - Trip + KM</field>

        <field name="company_id" ref="base.main_company"/>

        <field name="code">hybrid_trip_km</field>

        <field name="date_from">2025-01-01</field>

        <field name="base_rate">400</field>

        <field name="per_km_rate">5</field>

        <field name="active">True</field>

    </record>

</odoo>