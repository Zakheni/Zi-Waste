# Retired — Pastel Partner Bridge

This folder is **not** an Odoo module. The live Windows service is `sage_bridge` (repository root), bound to `127.0.0.1:8788`.

Do not start `main.py` in production. See `sage_bridge/docs/INSTALL.md` and `sage_bridge/docs/CUTOVER.md`.
